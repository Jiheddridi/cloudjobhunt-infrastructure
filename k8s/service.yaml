apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app: backend
spec:
  type: LoadBalancer  # ✅ IP publique Azure
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 80          # Port externe (ce que les utilisateurs appellent)
      targetPort: 8000  # Port interne du container FastAPI

